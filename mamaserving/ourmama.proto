syntax = "proto3";

import "alimama.proto";

package ourmama.proto;

message IntraReq {
    uint64 id = 1; // session id
    repeated uint64 offset = 2; // which task in the session
    repeated alimama.proto.SliceRequest slice_req = 3;
}

message IntraResp {
    uint64 id = 1;
    repeated uint64 offset = 2;
    repeated bytes data = 3;
}

service IntraService {
    rpc Get(IntraReq) returns (IntraResp) {}
}
